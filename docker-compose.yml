version : "1"


services:
  app :
    build: .
    ports:
      - "8080:9090"
    depends_on:
      - db
#      - redis


  db :
    image : postgres
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: testdb
    ports:
      - "5432:5432"


#
#  redis :
#    image: redis


